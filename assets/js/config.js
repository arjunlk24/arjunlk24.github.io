/* ============================================================
   config.js — the only file you'd need to touch if you ever
   rename your GitHub repo or username.
   ============================================================ */
const SITE_CONFIG = {
  owner: 'arjunlk24',
  repo: 'arjunlk24.github.io',
  branch: 'main',
  liveDataPath: 'data.json',
  draftDataPath: 'data-draft.json',
  imagesFolder: 'images',
};
